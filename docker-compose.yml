services:
  web:
    build: .
    environment:
      - LETSENCRYPT_HOST=jstet.net
      - VIRTUAL_HOST=jstet.net
    restart: always
    networks:
    - proxy
    - default
networks:
  proxy:
    external: true

